{
  "version": 2,
  "name": "bread",
  "alias": ["sourdough.now.sh", "kenyer.now.sh"],
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static-build",
      "config": { "distDir": "build" }
    }
  ]
}
